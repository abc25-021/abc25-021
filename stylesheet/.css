*{
        margin:0;
        padding:0;
        box-sizing:border-box;
      }
      #sitetitle{
        text-transform: uppercase;
        font-family: "Cloister Black",serif;
        display: flex;
         text-align: center;
        justify-content: center;
        gap:15px;
        margin: 0; /*remove default h1 margin*/
        font-size:24px;
        position:sticky;
        top:0;
        }
        .logo{
          width: auto;
          height: 40px;
          display:block;
        }
      body{
        margin:0;
        font-family: Arial, sans-serif;
      }
      #siteheader{
        background:#111;
        color:white;
        padding:15px 30px;
        text-align:centre;
        justify-content: space-between;
        position: sticky; /*add this*/
        z-index:10; /* add this*/
        position:sticky;
        top:0;
        display:flex;
      }
      #sitenav li:hover{
        background: transparent;
        margin:0;
        text-align: left;
      }
      #sitenav a:hover{
        background:#777
      }
      /*Make the nav list horizontal*/
      #ulnav{
        list-style-type: none; /*remove bullets*/
        margin:0;
        padding:0;
        display:flex;
        gap:25px; /*space between links*/
      }
      #ulnav a{
        color:white;
        text-decoration: none;
        padding: 8px 12px;
        font-family: Arial, Helvetica, sans-serif;
      }
      #ulnav a:hover{
        text-decoration: underline;
      }
      /* Video hero section*/
      .hero-video{
        position:relative;
        width:100%;
        height:calc(100hv-70px);
        overflow: hidden;
        background:#0000; /*fallback if vidoes don't load*/
      }
      .video-grid{
        display: grid;
        grid-template-columns: repeat(2,1fr); /*2x2 grid*/
        grid-template-rows: repeat(2,1fr);
        width: 100%;
        height: 50%;
          object-fit: cover; /*crops videos to fill space, no stretching*/
      }
        .hero-video video{
            width:100%;
            height:50%;
            object-fit: cover; /*crops videos to fill space, no stretching*/
        }
        /* Text overlay centered on top*/
        .info-overlay{
            position: absolute;
            top:0;
            left:0;
            width:100%;
            height: 100%;
            background: rgba(0,0,0,0.5); /*darkens videos so text is readable*/
            color: white;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 20px;
        }
        .info-overlay h2{
            font-size: 3rem;
            margin : 0 0 15px 0;
            text-transform: uppercase;
            font-family: "Cloister Black", serif;
        }
      .showcase{
        display:flex;
        align-items:stretch;
        min-height: calc(100vh-70px);
        background:#111;
      }
      .side-images{
        flex:1;
        display:flex;
        flex-direction:column;
        gap:0;
      }
      .side-images img{
        width:100%;
        height:50%;
        object-fit:cover;
        display:block;
      }
      .info-center{
        flex:1.2;
        background:rgba(0,0,0,0.8);
        color:white;
        display:flex;
        flex-direction:column;
        justify-content:center;
        align-items:center;
        text-align:center;
        padding:40px;
      }
      .info-center p{
        max-width:500px;
        font-size:1.1rem;
        line-height:1.7;
        font-family:Arial,sans-serif;
      }
      footer{
        background:#111;
        color:white;
        text-align:center;
        padding:20px 30px;
      }
      social-media images{
        align-items: bottom center;
        width: 0.1px;
        height:0.3px;
      }
      
